import React from 'react';
import { Container } from '@mui/material';
import Widgets from './Widgets';

const Dashboard = () => {
  return (
    <Container>
      <Widgets />
      {/* Ajoutez plus de contenu ici, comme des graphiques ou des tables */}
    </Container>
  );
};

export default Dashboard;
